{
  "name": "saucedemo-playwright",
  "version": "1.0.0",
  "description": "Playwright (JS) automation suite for saucedemo.com",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.0"
  }
}
